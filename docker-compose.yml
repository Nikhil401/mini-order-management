services:
  api-gateway:
    image: ${IMAGE_PREFIX:-mini-orders}/api-gateway:${IMAGE_TAG:-local}
    build:
      context: .
      dockerfile: src/ApiGateway/Dockerfile
    environment:
      ReverseProxy__Clusters__order-cluster__Destinations__order-destination__Address: http://order-service:8080/
      ReverseProxy__Clusters__inventory-cluster__Destinations__inventory-destination__Address: http://inventory-service:8080/
    ports:
      - "5000:8080"
    depends_on:
      - order-service
      - inventory-service

  ui:
    image: ${IMAGE_PREFIX:-mini-orders}/ui:${IMAGE_TAG:-local}
    build:
      context: ./ui
      dockerfile: Dockerfile
    ports:
      - "4200:80"
    depends_on:
      - api-gateway

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  rabbitmq:
    image: rabbitmq:3-management
    environment:
      RABBITMQ_DEFAULT_USER: "${RABBITMQ_USERNAME:-appuser}"
      RABBITMQ_DEFAULT_PASS: "${RABBITMQ_PASSWORD:-change-me}"
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_PID: Developer
      MSSQL_SA_PASSWORD: "${SQL_PASSWORD}"
    ports:
      - "1433:1433"
    volumes:
      - sqlserver-data:/var/opt/mssql

  inventory-service:
    image: ${IMAGE_PREFIX:-mini-orders}/inventory-service:${IMAGE_TAG:-local}
    build:
      context: . 
      dockerfile: src/InventoryService/Dockerfile
    environment:
      ConnectionStrings__Inventory: "Server=sqlserver,1433;Database=InventoryDb;User Id=sa;Password=${SQL_PASSWORD};Encrypt=False;TrustServerCertificate=True"
      Redis__ConnectionString: redis:6379
      RabbitMq__Host: rabbitmq
      RabbitMq__Port: 5672
      RabbitMq__UserName: "${RABBITMQ_USERNAME}"
      RabbitMq__Password: "${RABBITMQ_PASSWORD}"
    ports:
      - "5001:8080"
      - "5003:8081"
    depends_on:
      - redis
      rabbitmq:
        condition: service_healthy
      - sqlserver

  order-service:
    image: ${IMAGE_PREFIX:-mini-orders}/order-service:${IMAGE_TAG:-local}
    build:
      context: .
      dockerfile: src/OrderService/Dockerfile
    environment:
      ConnectionStrings__Orders: "Server=sqlserver,1433;Database=OrdersDb;User Id=sa;Password=${SQL_PASSWORD};Encrypt=False;TrustServerCertificate=True"
      Jwt__Issuer: "${JWT_ISSUER}"
      Jwt__Audience: "${JWT_AUDIENCE}"
      Jwt__Key: "${JWT_KEY}"
      InventoryService__BaseUrl: http://inventory-service:8080
      InventoryService__GrpcBaseUrl: http://inventory-service:8081
      RabbitMq__Host: rabbitmq
      RabbitMq__Port: 5672
      RabbitMq__UserName: "${RABBITMQ_USERNAME}"
      RabbitMq__Password: "${RABBITMQ_PASSWORD}"
    ports:
      - "5002:8080"
    depends_on:
      - inventory-service
      rabbitmq:
        condition: service_healthy
      - sqlserver

volumes:
  sqlserver-data:
