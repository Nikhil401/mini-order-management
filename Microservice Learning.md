Docker ? REST microservices ? API Gateway ? RabbitMQ ? Redis ? gRPC ? resilience ? Kubernetes ? Kafka ? distributed patterns.
-----------------------------------------------------------------------------------------------------------------------------

Docker: architecture, deamon, images, container, networks, volumes, .tar, docker compose yml, dockerfile, cmds
API Gateway(Client ? Microservices) routing : Ocelot, YARP
Between microservices(Interbnally): gRPC
Caching: Redis
Message queue(background processing, asynchronous messaging): RabbitMQ / Azure Service Bus
Event streaming(many consumers): Apache Kafka



Kubernetes basics: Pods, Deployments, Services, ConfigMaps, Secrets, Ingress, scaling
Resilience: retries, timeout, circuit breaker, idempotency
Distributed logging/tracing: Serilog, OpenTelemetry
Authentication/Authorization: JWT, OAuth2/OpenID Connect
Health checks
CI/CD basics with Azure DevOps or GitHub Actions

-----------------------------------------------------------------------------------------------------------------------------
Docker ? REST microservices ? API Gateway ? RabbitMQ ? Redis ? gRPC ? resilience ? Kubernetes ? Kafka ? distributed patterns.